import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    height: 56,
    width: '100%',
    borderRadius: 7,
    backgroundColor: "#000",
    justifyContent: 'center',
    alignItems: 'center'
  },
  title: {
    fontSize: 16,
    color: "#FFF"
  }
});